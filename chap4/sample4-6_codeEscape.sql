-- CREATEを学ぶ
-- ・テーブルを作成する

CREATE TABLE my_item(id INT, item_name TEXT, price INT);
-- CREATE TABLE 作成するテーブルの名前(カラム名 カラムの型(INT,TEXT など), カラム名 カラムの型(INT,TEXT など),…以下同文繰り返し);

-- データベースへ行って、「構造(structure)」へ飛べば、
-- 上の関数で作ったカラムがデータベース内にある。
